# Git Workflow Rule

1. **Local Verification First**: Every change must be tested locally (`npm run build` or local dev server) before notifying the user.
2. **No Unsolicited Push**: NEVER execute `git push` directly without explicit instruction or approval from the user.
3. **User Control**: The user will review changes locally and specify when they are ready to push to remote git branches.
