# Project Guidelines & Workflow Rules

## Git & Deployment Rules
- **NEVER run `git push` directly** without explicit permission from the user for any changes.
- All code edits must be verified in the local environment (`npm run dev` / `npm run build`) first.
- Wait for the user to test and verify changes locally before pushing to git.
